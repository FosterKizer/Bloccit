
<h1>New Summary</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for summaries:</p>
    <ul>
      <li>Summary needs to rhyme in some way</li>
      <li>Look happy</li>
      <li>Try to summarize</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @summary, url: topic_post_summary_path(@topic, @post) do |f| %>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter post summary" %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>

